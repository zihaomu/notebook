__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Large model files are mounted locally or supplied by the cloud template.
models/*.onnx
models/*.mxr
models/*.pt
models/*.gguf
models/*.log
models/*.part
models/.cache/
!models/README.md

# Keep the verified pipeline artifacts, ignore ad-hoc output elsewhere.
output/*
!output/.gitkeep
!output/pipeline/
!output/pipeline/**
