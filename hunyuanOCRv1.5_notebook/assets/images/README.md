# Synthetic demo images

The PNG files in this directory are generated by `scripts/generate_assets.py`.
They are original synthetic documents created for this demo and contain no
third-party document scans. Re-run the generator inside the notebook image to
reproduce them byte-for-byte.
